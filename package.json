{
  "name": "debugging-assessment",
  "version": "1.0.0",
  "description": "Simple note-taking application",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"docker-compose up\" \"npm --prefix api run start\" \"npm --prefix client run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
